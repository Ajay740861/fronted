import React from 'react'

const TextInput = ({ id, lable, type, disable}) => {
  return (
    <div>
        <label htmlFor={id}></label>
        <input id={id} type={type} disable={disabled}
    
        
        />
    </div>
  )
}

export default input